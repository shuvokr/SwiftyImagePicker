# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'SwiftyImagePicker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftyImagePicker

end

target 'SwiftyImagePickerExample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftyImagePickerExample
  platform :ios, '9.0'
  pod 'SwiftyImagePicker', :path => '.'

end
